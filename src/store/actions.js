const actions = {
  // addNAsync(context, step) {          //外部通过 this.$store.dispatch('addNAsync',参数) 调用
  //  setTimeout(() => {                 //异步操作
  //   context.commit('setToken',step)   // 实际修改state 还是通过 上下文的 commit  修改 例如：context.commit('setToken',step)
  //  }, 1000);
  // }
}
export default actions
