const getters = {
  // 包装数据 类似 computed
  // newToken(state) { //state是vuex上下文中的  state
  //   return `新的token是：${state.token}`
  // },
  // 外部使用方法：this.$store.getters.名称  例如：this.$store.getters.newToken
}
export default getters
